import { NodeModel, ToggleHandler, OpenHandler, CloseHandler, InitialOpen } from "../types";
export declare const useOpenIdsHelper: (tree: NodeModel[], initialOpen?: InitialOpen | undefined) => [(string | number)[], {
    handleToggle: ToggleHandler;
    handleCloseAll: () => void;
    handleOpenAll: () => void;
    handleOpen: OpenHandler;
    handleClose: CloseHandler;
}];
